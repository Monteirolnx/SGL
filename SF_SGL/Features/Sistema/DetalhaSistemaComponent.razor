@inject MediatR.IMediator _mediatr

@code {
    DetalhaSistema Detalhe;


    protected override async Task OnInitializedAsync()
    {
        DetalhaSistema.Query query = new();
        Detalhe.Data = await _mediatr.Send(query);
    }

}
